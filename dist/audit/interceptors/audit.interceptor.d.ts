import { NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';
import { Repository } from 'typeorm';
import { AuditLog } from '../entities/audit-log.entity';
import { TransactionLog } from '../entities/transaction-log.entity';
import { AuditService } from '../audit.service';
export declare class AuditInterceptor implements NestInterceptor {
    private readonly auditService;
    private readonly auditLogRepository;
    private readonly transactionLogRepository;
    private readonly logger;
    constructor(auditService: AuditService, auditLogRepository: Repository<AuditLog>, transactionLogRepository: Repository<TransactionLog>);
    intercept(context: ExecutionContext, next: CallHandler): Promise<any>;
    private getAuditMetadata;
    private createAuditLog;
    private updateAuditLog;
    private createTransactionLog;
    private updateTransactionLog;
    private logError;
    private logTransactionError;
    private isTransactionRequest;
    private getTransactionType;
    private getTransactionCategory;
    private extractAmount;
    private extractCurrency;
    private extractParticipants;
    private extractEnergyDetails;
    private extractContractDetails;
    private getUserId;
    private getSessionId;
    private getClientIp;
    private getUserAgent;
    private getAuthenticationMethod;
    private getPermissionsChecked;
    private getRolesChecked;
    private getCorrelationId;
    private getBatchId;
    private generateTransactionId;
    private sanitizeData;
    private isSensitiveField;
    private calculateChecksum;
    private isRecoverableError;
}
